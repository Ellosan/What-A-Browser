org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
# The browser is a native library with no Java of its own beyond the manifest,
# so none of the Jetpack machinery is wanted.
android.nonTransitiveRClass=true
